# Copier vers .env à la racine et ajuster si besoin
# Base PostgreSQL (voir docker-compose.yml)
DATABASE_URL="postgresql://plateforme:plateforme_dev@localhost:5432/plateforme"

# API Nest (backend/)
PORT=4000
CORS_ORIGIN=http://localhost:3000

# Auth JWT — à renforcer en production
JWT_SECRET=change-me-in-production
